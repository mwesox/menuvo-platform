# Environment Variables Example
#
# Per Turbo best practices, each app/package has its own .env.local file:
#   apps/api/.env.local      - Server-side vars (DATABASE_URL, secrets, etc.)
#   apps/console/.env.local  - VITE_* client vars
#   apps/shop/.env.local     - VITE_* client vars
#   packages/db/.env.local   - DATABASE_URL for Drizzle migrations
#
# Copy the relevant sections below to each location's .env.local file.
# See apps/*/src/env.ts for the full list of required/optional variables.

# ============================================================================
# apps/api/.env.local - Server-side configuration
# ============================================================================

# CORS allowed origins (comma-separated)
# Development: has default in env.ts, production: required
# ALLOWED_ORIGINS=https://console.menuvo.app,https://shop.menuvo.app

# Server URLs
# SERVER_URL=http://localhost:4000
# CONSOLE_URL=http://localhost:3000

# Database connection string (required)
DATABASE_URL=postgresql://user:password@localhost:5432/menuvo

# Cookie domains for cross-subdomain sharing (production only)
# Set to ".menuvo.app" in production for console.menuvo.app ↔ api.menuvo.app
# COOKIE_DOMAIN=.menuvo.app

# OpenRouter AI (required for AI-powered menu extraction)
OPENROUTER_API_KEY=sk-or-v1-...

# S3-Compatible Storage
# S3_ENDPOINT=https://s3.amazonaws.com
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=
# S3_BUCKET=menuvo-images
# S3_REGION=eu-central-1
# S3_BASE_URL=https://cdn.menuvo.app
# S3_FILES_BUCKET=menuvo-files

# Encryption (required for OAuth token storage)
# Generate with: openssl rand -hex 32
# Must be exactly 64 hex characters (32 bytes / 256 bits)
ENCRYPTION_KEY=

# Mollie Payment Processing (EU)
# MOLLIE_API_KEY=
# MOLLIE_CLIENT_ID=
# MOLLIE_CLIENT_SECRET=
# MOLLIE_REDIRECT_URI=https://console.menuvo.app/settings/payments/callback
# MOLLIE_ORG_ACCESS_TOKEN=
# MOLLIE_TEST_MODE=true
# MOLLIE_SKIP_ONBOARDING_CHECK=false

# Brevo Email
# BREVO_API_KEY=

# Menu Import AI Model (OpenRouter model ID)
# MENU_IMPORT_MODEL_ID=nvidia/nemotron-3-nano-30b-a3b:free
# MENU_IMPORT_MODEL_STRUCTURED=false

# AI Recommendations Model (OpenRouter model ID)
# AI_RECOMMENDATIONS_MODEL_ID=openai/gpt-oss-safeguard-20b

# ============================================================================
# apps/console/.env.local - Console client-side configuration
# ============================================================================

# API URL (default: http://localhost:4000)
# VITE_API_URL=http://localhost:4000

# Worker URL for direct API access (bypassing Vite proxy)
# VITE_WORKER_URL=http://localhost:4000

# Sentry error tracking (optional)
# VITE_SENTRY_DSN=

# ============================================================================
# apps/shop/.env.local - Shop client-side configuration
# ============================================================================

# API URL (default: http://localhost:4000)
# VITE_API_URL=http://localhost:4000

# Sentry error tracking (optional)
# VITE_SENTRY_DSN=

# ============================================================================
# packages/db/.env.local - Database migrations
# ============================================================================

# Database connection string (required for Drizzle migrations)
DATABASE_URL=postgresql://user:password@localhost:5432/menuvo
