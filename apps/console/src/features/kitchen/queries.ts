/**
 * Kitchen-specific query hooks.
 *
 * Extends base order queries with kitchen-specific behavior
 * like offline mutation queueing and silent status updates.
 *
 * Custom hooks have been removed - use direct tRPC patterns in components.
 * For kitchen-specific behavior (offline queueing), implement inline in components.
 */
