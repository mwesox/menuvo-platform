# =============================================================================
# PRODUCTION CONFIGURATION (non-secrets only)
# Secrets remain in GitHub Secrets, passed via deploy workflow
# =============================================================================

# Image Worker
IMAGE_WORKER_CONCURRENCY=4
