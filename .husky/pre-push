echo "🔍 Running biome check..."
bun run check
if [ $? -ne 0 ]; then
  echo "❌ Biome check failed. Push aborted."
  exit 1
fi

echo "🔍 Running TypeScript check..."
bunx --bun tsc --noEmit
if [ $? -ne 0 ]; then
  echo "❌ TypeScript check failed. Push aborted."
  exit 1
fi

echo "🔍 Checking database migrations..."
bunx drizzle-kit check
if [ $? -ne 0 ]; then
  echo "❌ Database schema out of sync. Run 'bun run db:generate' to create migration."
  exit 1
fi

echo "✅ All checks passed."
