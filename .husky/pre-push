echo "🔍 Running biome check..."
bun run check
if [ $? -ne 0 ]; then
  echo "❌ Biome check failed. Push aborted."
  exit 1
fi

echo "🔍 Running TypeScript check..."
bun run check-types
if [ $? -ne 0 ]; then
  echo "❌ TypeScript check failed. Push aborted."
  exit 1
fi

echo "🔨 Running build (catches stricter type errors)..."
bun run build
if [ $? -ne 0 ]; then
  echo "❌ Build failed. Push aborted."
  exit 1
fi

echo "✅ All checks passed."
